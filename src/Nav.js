export default function Nav(){
    return(
        <nav className="flex-center">
            <span>Where in the world?</span>
            <span className="theme-button">Dark Theme</span>
        </nav>
    );
}